# Word-Embeddings-MLPs
Built a GPT model from scratch using character-level bigram, trigram, and multi-layer perceptron models. Covers word embeddings, softmax, cross-entropy loss, backpropagation, and hyperparameter tuning.
